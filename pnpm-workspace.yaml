# pnpm 10+ reads settings from here, not the package.json "pnpm" field.
# Allow esbuild (tsx's engine) to run its install script.
allowBuilds:
  esbuild: true
# Don't block `pnpm run <script>` on a deps-status precheck.
verifyDepsBeforeRun: false
